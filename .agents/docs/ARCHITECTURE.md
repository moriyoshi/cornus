# System architecture

> **Moved.** The canonical architecture document now lives at the repository
> root: [`ARCHITECTURE.md`](../../ARCHITECTURE.md).
>
> It was promoted to a root, human-reader-ready document on 2026-07-05. The agent
> tooling that previously maintained this file — the `distill-memories` skill,
> `AGENTS.md`, and the LTM / JOURNAL references — now targets the root document.
> Edit `../../ARCHITECTURE.md` directly; do not re-expand this stub.
