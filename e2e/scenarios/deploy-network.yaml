# Two services on the implicit default network. srv publishes NO ports —
# reaching it by bare name from cli proves the headless-Service DNS baseline.
services:
  srv:
    image: nginx:alpine
  cli:
    image: alpine:3.20
    command: ["sleep", "3600"]
