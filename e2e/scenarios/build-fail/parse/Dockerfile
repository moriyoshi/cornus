# A syntactically invalid Dockerfile: the frontend must reject it at parse time,
# before any step runs, and the failure must surface as a BUILD FAILED trailer.
THIS IS NOT A VALID INSTRUCTION
@@@ %%% garbage
